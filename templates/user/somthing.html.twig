<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %} My Symfony Website {% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            <!-- CSS only -->
            <link href="{{ asset('css/customerbase.css') }}" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous" >
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block nav %}
            <div id="container">
            <ul id="menu">
                <li><a class="nav-item nav-link" href="#">Home</a></li>
	            <li><a class="nav-item nav-link" href="#">User</a></li>
	            <li><a class="nav-item nav-link" href="#">Product</a></li>
	            {# <li><a href="view_all_category">Category</a>
                    <ul>
                        {% for category in categories %}
                        <li><a href="{{path('view_category', {'id':category.id})}}">{{category.CategoryName}}</a></li>
                        {% endfor %}
                    </ul>
                </li> #}
                <li>
					<a class="nav-item nav-link" href="{{path('view_all_order')}}">Order</a>
				</li>
				<li>
					<a class="nav-item nav-link" href="{{path('app_logout')}}">Logout</a>
				</li>
            </ul>
            </div>
        {% endblock %}
        {% block body %}{% endblock %}
    </body>
</html>